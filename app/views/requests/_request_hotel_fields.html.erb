<div class="nested-fields">
  <%= f.fields_for :spot, Spot.new do |spot| %>
    <%= spot.input :name, input_html: { class: 'gPlaceAutoComplete' }  %>
  <%= link_to_remove_association 'remove_request_spot', spot %>
  <% end %>
</div>

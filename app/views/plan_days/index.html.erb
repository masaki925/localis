<h1>Listing plan_days</h1>

<table>
  <tr>
    <th>Plan</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @plan_days.each do |plan_day| %>
  <tr>
    <td><%= plan_day.plan_id %></td>
    <td><%= link_to 'Show', plan_day %></td>
    <td><%= link_to 'Edit', edit_plan_day_path(plan_day) %></td>
    <td><%= link_to 'Destroy', plan_day, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Plan day', new_plan_day_path %>
